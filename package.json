{
  "name": "desafio-busca-luizalabs",
  "version": "1.0.0",
  "description": "A command-line program to search for a sentence keywords in multiple files",
  "main": "src/app/search.js",
  "scripts": {
    "search": "node dist/app/search.js",
    "build": "rm -rf dist/ && npx tsc",
    "prepare": "node dist/app/createIndex.js",
    "build-search": "rm -rf dist/ && npx tsc && node dist/app/search.js",
    "test": "jest",
    "watch": "jest --watch",
    "coverage": "jest --coverage",
    "clear": "rm -rf dist/"
  },
  "author": "Gabriel Nascimento",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/node": "^20.2.5",
    "eslint-config-standard-with-typescript": "^35.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.1.3"
  }
}
